/// Laravel cow feed endpoints. Not implemented — structure only.
class FeedService {
  // GET  {{laravel_url}}/cows/{{cow_id}}/feed
  // POST {{laravel_url}}/cows/{{cow_id}}/feed
  // POST {{laravel_url}}/cows/{{cow_id}}/feed-from-image
}
