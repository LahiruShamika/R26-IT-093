/// Laravel cow endpoints. Not implemented — structure only.
class CowService {
  // GET    {{laravel_url}}/cows
  // GET    {{laravel_url}}/cows/public
  // POST   {{laravel_url}}/cows
  // GET    {{laravel_url}}/cows/{{cow_id}}
  // PUT    {{laravel_url}}/cows/{{cow_id}}
  // DELETE {{laravel_url}}/cows/{{cow_id}}
  // POST   {{laravel_url}}/cows/identify
}
